# Distill π0.5 → BaseController
teacher: pi0.5
synthetic: false
checkpoint: gs://openpi-assets/checkpoints/pi05_base
steps: 10000
batch_size: 64
n_samples: 16384
lr: 0.001
beta: 0.1          # β in L_KD
lambda_feat: 0.5   # λ_feat in L_distill
device: cuda
export: checkpoints/base_controller_w0.bin
seed: 0
